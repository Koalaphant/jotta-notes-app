{
  "name": "jotta-notes-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd jotta-notes-server && npm install) || (cd jotta-notes-client && npm install)",
    "server": "node ./jotta-notes-server/server.js",
    "client": "cd jotta-notes-client && npm run dev"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}