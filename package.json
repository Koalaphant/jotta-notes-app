{
  "name": "jotta-notes-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "cd jotta-notes-client && npm install && npm run build && cd ../jotta-notes-server && npm install",
    "server": "cd jotta-notes-server && npm install && node server.js",
    "client": "cd jotta-notes-client && npm run dev"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}